{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

    "DashboardApi": {
        "BaseUrl": "http://localhost:5219/"
    },

  "AuthMock": {
    "Enabled": false
  },

    "ConnectionStrings": {
        // Optional (recommended for multi-instance / IIS recycle resilience): SQL Server distributed cache for session storage.
        // Requires a table created via:
        //   dotnet sql-cache create "<CONN_STRING>" dbo SessionCache
        "SessionDb": "Data Source=WIDEVDB32,1533;Database=UAT_YoneticiRaporu;Integrated Security=True;TrustServerCertificate=True"
        //
        // Optional alternative: use Redis for session storage.
        // "Redis": "localhost:6379"
    },

  "AllowedHosts": "*"
}
